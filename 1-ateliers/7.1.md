# Atelier 7.1 : Web APIs et PWA

1. Reprenez la [correction de l'exercice 6.1 en TypeScript](./corrections/6.1/) ou [la version JS exercice 4.1](./corrections/4.1/)

2. Modifiez la fonction `findUsers()` pour stocker les utilisateurs dans le cache nommé `users-github-v1`

3. Modifiez à nouveau la fonction `findUsers()` afin de lancer la requête réseau avec `fetch` uniquement lorsqu'il n'y a pas les données dans le cache
- Si la donnée est en cache, retourne-la directement
- Sinon exécutez le `fetch` en clonant la réponse (`response.clone()`) et en enregistrant les données dans le cache avec `cache.put()` avant de les retourner. Voici un extrait de code du clone de la réponse.
```js
fetch(url).then((response) => {
  const cloneReponse = response.clone(); // avant de faire .json() => utilisation ou consommation de la ressource
  return response.json()
})
```

4. Écrivez une fonction `getUserCoordinates()` qui retourne une `Promise` résolue avec les coordonnées `{ latitude, longitude }` obtenues via `navigator.geolocation.getCurrentPosition()`.

5. Gèrez proprement le rejet de la promesse `getUserCoordinates()` en capturant les erreurs courantes (`PERMISSION_DENIED`, `POSITION_UNAVAILABLE`, `TIMEOUT`).

6. Créez un fichier `index.html` et importez la fonction `getUserCoordinates()` pour l"utiliser afin de tester votre programme

---

## Bonus 

7. Calculez la distance séparant l'utilisateur d'un point fixe (ex: Paris : 48.8566, 2.3522) et affichez le résultat en kilomètres dans la console ou le DOM à partir de cette fonction 
```javascript
function calculerDistanceHaversine(lat1, lon1, lat2, lon2) {
    const R = 6371; // Rayon de la Terre en kilomètres
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
              Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
              Math.sin(dLon / 2) * Math.sin(dLon / 2);
              
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    
    return R * c; // Distance en km
}

```

8. Externalisez `findUsers()` et `getUserCoordinates()` dans les web workers.
9. Créez une fonction `loadAssetsFromCache`, un service worker qui intercepte les requêtes ***"/", "/index.html", "/styles.css", "/app.js"*** pour les mettre en cache.